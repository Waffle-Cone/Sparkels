class Project {
  //task objects list
  tasks = [];

  constructor(id, name, description) {
    this.id = id;
    this.name = name;
    this.description = description;
  }
  //methods
}

export default Project;
